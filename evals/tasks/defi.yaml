[
  {
    "id": "defi.balance_check.001",
    "messages": [{"role": "user", "content": "What is the USDC balance of vitalik.eth on Base?"}],
    "expected_tool_calls": ["resolve_ens", "get_erc20_balance"],
    "expected_text_contains": ["USDC", "vitalik"],
    "max_duration_ms": 8000,
    "max_cost_usdc": 500000,
    "scorer": "contains",
    "rubric": "Resolves ENS and returns a USDC balance."
  },
  {
    "id": "defi.lending_rates.001",
    "messages": [
      {
        "role": "user",
        "content": "Compare USDC supply and borrow APY on Aave v3 vs Compound v3 on Base mainnet."
      }
    ],
    "expected_tool_calls": ["get_lending_rates"],
    "expected_text_contains": ["Aave", "Compound", "USDC", "%"],
    "max_duration_ms": 12000,
    "max_cost_usdc": 600000,
    "scorer": "llm_judge",
    "rubric": "Lists concrete supply APY and borrow APY percentages for USDC on both Aave v3 and Compound v3 on Base mainnet. Cites specific numbers for both protocols so the user can compare them directly."
  },
  {
    "id": "defi.protocol_tvl.001",
    "messages": [
      {"role": "user", "content": "What is the current TVL of the Aave protocol?"}
    ],
    "expected_tool_calls": ["get_protocol_tvl"],
    "expected_text_contains": ["Aave", "TVL", "$"] ,
    "max_duration_ms": 10000,
    "max_cost_usdc": 500000,
    "scorer": "contains",
    "rubric": "Calls get_protocol_tvl and cites the current TVL figure."
  },
  {
    "id": "defi.eth_primitive_fees.001",
    "messages": [
      {
        "role": "user",
        "content": "TASK_CLASS: eth_primitive_fees. Analyze liquity, railgun, tornado-cash, aave-v3, uniswap, lido, and makerdao using one batched get_protocol_tvl call with include_historical=true and days=30, plus one get_token_price call for LQTY, RAIL, TORN, AAVE, UNI, LDO, and MKR in USD. Verify that the returned protocol slug set contains all seven requested slugs; if a slug is absent, mark its fields DATA_GAP and do not infer that the batch was truncated unless the tool explicitly reports truncation. Return the exact JSON contract first, then --- and a short summary. Preserve nulls as DATA_GAP."
      }
    ],
    "expected_tool_calls": ["get_protocol_tvl", "get_token_price"],
    "expected_tool_args": {
      "get_protocol_tvl": {
        "protocols": ["liquity", "railgun", "tornado-cash", "aave-v3", "uniswap", "lido", "makerdao"],
        "include_historical": true,
        "days": 30
      },
      "get_token_price": {
        "tokens": ["LQTY", "RAIL", "TORN", "AAVE", "UNI", "LDO", "MKR"],
        "vs_currency": "usd"
      }
    },
    "expected_text_contains": ["eth_primitive_fees", "protocols", "basket_summary"],
    "max_duration_ms": 60000,
    "max_cost_usdc": 1000000,
    "scorer": "llm_judge",
    "rubric": "Calls each required tool exactly once with the requested basket, verifies all seven returned protocol slugs before labeling, emits a valid eth_primitive_fees JSON object before any summary text, includes all seven protocol rows, and records missing upstream fields explicitly instead of fabricating values or attributing omissions to truncation without tool evidence."
  },
  {
    "id": "defi.entry_timing.001",
    "messages": [
      {
        "role": "user",
        "content": "TASK_CLASS: entry_timing. OUTPUT_CONTRACT: entry_timing. Analyze the fixed ten-token basket using exactly one get_token_price_historical call with days=90, stats_only=true and one get_token_price call in USD. Return the contract JSON first, then --- and a report."
      }
    ],
    "expected_tool_calls": ["get_token_price_historical", "get_token_price"],
    "expected_tool_args": {
      "get_token_price_historical": {
        "tokens": ["proton-loan", "gains-network", "nosana", "liquity", "chain-2", "tornado-cash", "railgun", "aixbt", "venice-token", "orca"],
        "days": 90,
        "vs_currency": "usd",
        "stats_only": true
      },
      "get_token_price": {
        "tokens": ["proton-loan", "gains-network", "nosana", "liquity", "chain-2", "tornado-cash", "railgun", "aixbt", "venice-token", "orca"],
        "vs_currency": "usd"
      }
    },
    "expected_text_contains": ["entry_timing", "tokens", "actionable"],
    "expected_json_shape": {"task_class": null, "schema_version": null, "tokens": null, "actionable": null},
    "max_duration_ms": 60000,
    "max_cost_usdc": 1000000,
    "scorer": "json_shape",
    "rubric": "Calls both market-data tools exactly once, emits the entry_timing JSON contract before the summary, preserves null data gaps, and provides exactly ten token rows."
  },
  {
    "id": "defi.stablecoin_yield_compare.001",
    "messages": [
      {
        "role": "user",
        "content": "TASK_CLASS: stablecoin_yield_compare. OUTPUT_CONTRACT: stablecoin_yield_compare. Compare the fixed decentralized and centralized stablecoin baskets using exactly one get_yield_rates call and one get_token_price call. Return the contract JSON first, then --- and a report."
      }
    ],
    "expected_tool_calls": ["get_yield_rates", "get_token_price"],
    "expected_tool_args": {
      "get_yield_rates": {
        "stable_only": true,
        "max_apy": 30,
        "min_tvl_usd": 1000000,
        "limit": 50,
        "symbols_any": ["LUSD", "BOLD", "crvUSD", "GHO", "DAI", "USDT", "USDC", "PYUSD", "RLUSD", "USDG", "USD1", "FDUSD", "TUSD", "USDP", "USDS"]
      },
      "get_token_price": {
        "tokens": ["LUSD", "BOLD", "crvUSD", "GHO", "DAI", "USDT", "USDC", "PYUSD", "RLUSD", "USDG", "USD1", "FDUSD", "TUSD", "USDP", "USDS"],
        "vs_currency": "usd"
      }
    },
    "expected_text_contains": ["stablecoin_yield_compare", "stables", "basket_metrics"],
    "expected_json_shape": {"task_class": null, "schema_version": null, "stables": null, "basket_metrics": null, "prediction": null},
    "max_duration_ms": 60000,
    "max_cost_usdc": 1000000,
    "scorer": "json_shape",
    "rubric": "Calls both required tools exactly once, emits the stablecoin_yield_compare JSON contract before the summary, preserves nulls and gaps, and reports the fixed fifteen-symbol basket."
  },
  {
    "id": "defi.chain_health_landscape.001",
    "messages": [
      {
        "role": "user",
        "content": "Compare Ethereum, Arbitrum, Base, Solana, and Avalanche by current TVL, 7-day and 30-day TVL change, and recent aggregate fees. Return compact JSON with task_class chain_health_landscape, one row per chain, explicit missing-data fields, and a short conclusion about ecosystem momentum."
      }
    ],
    "expected_tool_calls": ["get_chain_metrics"],
    "expected_tool_args": {
      "get_chain_metrics": {
        "chains": ["Ethereum", "Arbitrum", "Base", "Solana", "Avalanche"],
        "days": 30
      }
    },
    "expected_text_contains": ["chain_health_landscape", "TVL", "fees"],
    "max_duration_ms": 45000,
    "max_cost_usdc": 700000,
    "scorer": "llm_judge",
    "rubric": "Calls get_chain_metrics for the requested ecosystems, reports current TVL plus 7-day and 30-day changes and aggregate fees when available, labels missing upstream fields instead of fabricating them, and gives a concise comparison."
  },
  {
    "id": "defi.dex_volume_landscape.001",
    "messages": [
      {
        "role": "user",
        "content": "Compare Uniswap V3, Curve DEX, and PancakeSwap by 24-hour, 7-day, and 30-day volume, period-over-period changes, and share of reported DEX volume. Return compact JSON with task_class dex_volume_landscape, one row per protocol, and a brief conclusion."
      }
    ],
    "expected_tool_calls": ["get_dex_volume"],
    "expected_tool_args": {
      "get_dex_volume": {
        "protocols": ["uniswap-v3", "curve-dex", "pancakeswap"],
        "lookback_days": 30,
        "limit": 10
      }
    },
    "expected_text_contains": ["dex_volume_landscape", "24", "volume", "share"],
    "max_duration_ms": 30000,
    "max_cost_usdc": 600000,
    "scorer": "llm_judge",
    "rubric": "Calls get_dex_volume with the requested protocols, reports all three volume windows and changes, explains that shares use the unfiltered reported DEX denominator, and labels protocols with unavailable data."
  },
  {
    "id": "defi.consistent_yield.001",
    "messages": [
      {
        "role": "user",
        "content": "Where is the best consistent stablecoin yield in DeFi currently? Don't include spikes or short-term rates."
      }
    ],
    "expected_tool_calls": ["get_yield_rates"],
    "expected_text_contains": ["30d", "USDC", "%"],
    "max_duration_ms": 15000,
    "max_cost_usdc": 700000,
    "scorer": "llm_judge",
    "rubric": "Recommends stablecoin yield based on 30-day or longer trailing return data. Avoids protocols or rates that are explicitly short-term, promotional, or spiky. The recommendation is actionable for a risk-averse user. The agent should call get_yield_rates with stable_only=true and max_apy=30 to exclude leveraged/boosted pools."
  },
  {
    "id": "defi.stablecoin_basket_compare.001",
    "messages": [
      {
        "role": "user",
        "content": "Compare current stablecoin yields and peg health for decentralized LUSD, BOLD, crvUSD, GHO, DAI versus centralized USDT, USDC, PYUSD, RLUSD, USDG, USD1, FDUSD, TUSD, USDP, USDS. Call get_yield_rates once with stable_only=true, max_apy=30, min_tvl_usd=1000000 and all symbols, and call get_token_price once for all symbols in USD. Return compact JSON with task_class stablecoin_yield_compare_test, per-token data gaps, basket average APYs, spread, peg-health counts, and a discrete decentralized/centralized/tie winner."
      }
    ],
    "expected_tool_calls": ["get_yield_rates", "get_token_price"],
    "expected_tool_args": {
      "get_yield_rates": {
        "stable_only": true,
        "max_apy": 30,
        "min_tvl_usd": 1000000
      },
      "get_token_price": {
        "vs_currency": "usd"
      }
    },
    "expected_text_contains": ["stablecoin_yield_compare_test", "basket", "spread"],
    "expected_text_not_contains": ["truncated", "retrieve the full pool list"],
    "expected_json_shape": {
      "task_class": null,
      "stables": null,
      "spread": null,
      "winner": null
    },
    "max_duration_ms": 120000,
    "max_cost_usdc": 100000,
    "scorer": "json_shape",
    "rubric": "Calls both market-data tools and emits a visible, compact stablecoin basket comparison rather than completing with empty text. Live values may vary; missing source data must be labeled rather than fabricated."
  },
  {
    "id": "defi.price_history_metrics.001",
    "messages": [
      {
        "role": "user",
        "content": "Assess ETH's recent 30-day high and volatility, and compare its current price with the preceding 90-day weekly-buy baseline. Use the historical price tool's precomputed metrics and report any partial-history flag."
      }
    ],
    "expected_tool_calls": ["get_token_price_historical"],
    "expected_tool_args": {
      "get_token_price_historical": {
        "tokens": ["ETH"],
        "days": 90,
        "stats_only": true
      }
    },
    "expected_text_contains": ["volatility", "90-day"],
    "max_duration_ms": 15000,
    "max_cost_usdc": 700000,
    "scorer": "llm_judge",
    "rubric": "Calls get_token_price_historical with days=90 and stats_only=true, uses high_30d and std_30d directly, compares price_end with dca_baseline_90d, and explicitly reports dca_baseline_90d_partial or missing data without fabricating values."
  },
  {
    "id": "defi.read_contract.caller_context.001",
    "messages": [
      {
        "role": "user",
        "content": "Use read_contract, not a convenience balance tool, to call the read-only balanceOf(address) view on the Ethereum USDC contract 0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48 for wallet 0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045 at the latest block. Pass the wallet as caller_address and use args_json for the balanceOf argument. Return the balance and state the caller context."
      }
    ],
    "expected_tool_calls": ["read_contract"],
    "expected_tool_args": {
      "read_contract": {
        "caller_address": "0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045"
      }
    },
    "expected_text_contains": ["balance", "caller"],
    "max_duration_ms": 15000,
    "max_cost_usdc": 700000,
    "scorer": "llm_judge",
    "rubric": "Calls read_contract with caller_address context, uses a view-only ABI, reports the returned balance, and does not claim that a transaction was signed or submitted."
  },
  {
    "id": "defi.lending_rates.usds_registry.001",
    "messages": [
      {
        "role": "user",
        "content": "Compare the current USDS supply and borrow APYs on Aave v3 and Compound v3 on Ethereum mainnet. Query both protocols for the USDS asset, report the live APYs or clearly label any missing data, and do not invent rates."
      }
    ],
    "expected_tool_calls": ["get_lending_rates"],
    "expected_tool_args": {
      "get_lending_rates": {
        "protocol": "all",
        "chain_id": 1,
        "assets": ["USDS"]
      }
    },
    "expected_text_contains": ["USDS", "Aave", "Compound", "APY"],
    "max_duration_ms": 15000,
    "max_cost_usdc": 700000,
    "scorer": "llm_judge",
    "rubric": "Calls get_lending_rates with protocol=all, chain_id=1, and assets=[USDS]. Compares Aave v3 and Compound v3 supply and borrow APYs using live values when available, and labels missing or partial data instead of fabricating rates."
  }
]
